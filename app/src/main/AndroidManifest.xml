<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newpage">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/electrical_scooter_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/electrical_scooter_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewPage">
        <activity android:name=".ActivityB1"></activity>
        <activity android:name=".ActivityD" />
        <activity android:name=".ActivityC" />
        <activity
            android:name=".ActivityB"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".ActivityC1" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <service android:name=".MyServis"/>
    </application>

</manifest>